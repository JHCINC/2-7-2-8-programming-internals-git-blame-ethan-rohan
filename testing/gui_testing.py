import tkinter as tk
from tkinter import messagebox
from tkinter import ttk

window = tk.Tk()
window.geometry("400x401")
window.config(bg="#000099")
window.resizable(width=True,height=True)
window.title('Note Taking App')
window.state('zoomed')

window.mainloop()
